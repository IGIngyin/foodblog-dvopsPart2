services:
    build-rms:
        build:
            context: .
            dockerfile: Dockerfile
        image: ingyinkhai304/fb-docker-img:v2
        container_name: fb-docker-container
        ports:
            - "8000:5050"
